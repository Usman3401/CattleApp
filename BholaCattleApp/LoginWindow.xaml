<Window x:Class="BholaCattleApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BholaCattleApp"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen"
        Title="Log in" Height="500" Width="800" ResizeMode="NoResize" 
        Background="{StaticResource BackgroundBrush}">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="200">
            <TextBlock Text="Log in" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <TextBlock Text="Username :" />
            <TextBox Text="{Binding Username}" Margin="0,0,0,10"/>
            <TextBlock Text="Password :" />
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,10"/>
            
            <Button x:Name="btn_login" Command="{Binding LoginCommand}" Content="Log In" BorderThickness="0" Foreground="White" FontWeight="Medium" FontFamily="Myriad Pro">
                <Button.Template>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Width="100" Height="24" CornerRadius="5" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background">
                            <Setter.Value>
                                <LinearGradientBrush StartPoint="0.5 1" EndPoint="0.5 0">
                                    <GradientStop Color="#58c0ce" Offset="0.712"/>
                                    <GradientStop Color="#5d9abb" />
                                </LinearGradientBrush>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0.5 1" EndPoint="0.5 0">
                                            <GradientStop Color="#50b8d6" Offset="0.712"/>
                                            <GradientStop Color="#4c9dbb" />
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0.5 1" EndPoint="0.5 0">
                                            <GradientStop Color="#45a4b0" Offset="0.712"/>
                                            <GradientStop Color="#4586a6" />
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            
        </StackPanel>
    </Grid>
</Window>
