<UserControl x:Class="BholaCattleApp.Views.AddEditTransactionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BholaCattleApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="{Binding Title}" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,20"/>
       
            <TextBlock Text="Transaction Date:" Margin="0,0,0,5"/>
            <DatePicker SelectedDate="{Binding TransDate}" Margin="0,0,0,10"/>
            
            <TextBlock Text="Type:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Type}" Margin="0,0,0,10"/>
            
            <TextBlock Text="Item:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding ItemID}" Margin="0,0,0,10"/>
            
            <TextBlock Text="Quantity:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Qty}" Margin="0,0,0,10"/>

            <TextBlock Text="Price:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Price}" Margin="0,0,0,10"/>
            
            <TextBlock Text="Total Amount:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding TotalAmount}" Margin="0,0,0,10"/>
            
            <TextBlock Text="Note:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Note}" Margin="0,0,0,20" Height="50" AcceptsReturn="True"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Save" Command="{Binding SaveCommand}" Width="80" Margin="0,0,10,0"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
