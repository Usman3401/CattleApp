<UserControl x:Class="BholaCattleApp.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BholaCattleApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="Home" FontSize="24" HorizontalAlignment="Center" Margin="0,10"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                <Button Content="{Binding TotalAnimals}" Margin="5"/>
                <!-- e.g., Total Animals = 25 -->
                <Button Content="{Binding MilkingCows}" Margin="5"/>
                <Button Content="{Binding Pregnant}" Margin="5"/>
            </StackPanel>
            <TextBlock Text="Recent Transaction" FontSize="18" Margin="10"/>
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="5">
                    <TextBlock Text="Purchase" FontWeight="Bold"/>
                    <TextBlock Text="{Binding PurchaseFeed}"/>
                    <TextBlock Text="{Binding PurchaseMedicine}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="5">
                    <TextBlock Text="Sell" FontWeight="Bold"/>
                    <TextBlock Text="{Binding SellMilk}"/>
                    <TextBlock Text="{Binding SellMeat}"/>
                </StackPanel>
            </Grid>
            <TextBlock Text="Report" FontSize="18" Margin="10"/>
            <StackPanel Margin="10">
                <TextBlock Text="Detail report of an animal"/>
                <TextBlock Text="Summary report of a animal"/>
                <TextBlock Text="Summary report of all animal"/>
                <Button Content="Print" Command="{Binding PrintReportCommand}" HorizontalAlignment="Right"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
