<UserControl x:Class="BholaCattleApp.Views.AddEditStatusAnimalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BholaCattleApp.Views"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      HorizontalContentAlignment="Center"
                      VerticalContentAlignment="Center"
                      Margin="0 12 0 12">

            <Border Background="White"
                    CornerRadius="12"
                    Padding="36,32"
                    Width="640"
                    Margin="20"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.15"/>
                </Border.Effect>

                <StackPanel>

                    <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding Message, Mode=OneWay}"
                               FontSize="13" Foreground="Gray"
                               HorizontalAlignment="Center" Margin="0,6,0,26"
                               TextWrapping="Wrap"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldPanel}">
                            <TextBlock Text="Animal:" Style="{StaticResource FieldLabel}"/>
                            <ComboBox ItemsSource="{Binding AnimalOptions}"
                                      SelectedValuePath="ANIMALID"
                                      SelectedValue="{Binding AnimalID, Converter={StaticResource DecimalToIntConverter}}"
                                      Style="{StaticResource ComboBoxInput}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DISPLAY}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Style="{StaticResource FieldPanel}" Margin="0,0,0,16">
                            <TextBlock Text="Status:" Style="{StaticResource FieldLabel}"/>
                            <ComboBox ItemsSource="{Binding StatusOptions}"
                                      SelectedValuePath="STATUSID"
                                      SelectedValue="{Binding StatusID, Converter={StaticResource DecimalToIntConverter}}"
                                      Style="{StaticResource ComboBoxInput}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding NAME}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldPanel}">
                            <TextBlock Text="Start Date:" Style="{StaticResource FieldLabel}"/>
                            <DatePicker SelectedDate="{Binding StartDate}" Style="{StaticResource DatePickerInput}"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="1" Style="{StaticResource FieldPanel}" Margin="0,0,0,16">
                            <TextBlock Text="End Date:" Style="{StaticResource FieldLabel}"/>
                            <DatePicker SelectedDate="{Binding EndDate}" Style="{StaticResource DatePickerInput}"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Style="{StaticResource FieldPanel}" Margin="0,0,0,16">
                        <TextBlock Text="Notes:" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding Notes}" Style="{StaticResource TextBoxInput}"
                                 Height="60" AcceptsReturn="True" TextWrapping="Wrap"
                                 MaxLength="50"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                        <Button Content="Save" Command="{Binding SaveCommand}"
                                Style="{StaticResource button}" MinWidth="90" Margin="0,0,10,0"/>
                        <Button Content="Cancel" Command="{Binding CancelCommand}"
                                Style="{StaticResource button}" MinWidth="90"/>
                    </StackPanel>

                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>