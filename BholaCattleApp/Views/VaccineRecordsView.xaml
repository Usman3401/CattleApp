<UserControl x:Class="BholaCattleApp.Views.VaccineRecordsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BholaCattleApp.Views"
             mc:Ignorable="d" Background="White"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Vaccine" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
                <Button Content="Add" Command="{Binding AddVaccineCommand}" Width="80" Margin="5"/>
                <Button Content="Edit" Command="{Binding EditVaccineCommand}" Width="80" Margin="5"/>
                <Button Content="Delete" Command="{Binding DeleteVaccineCommand}" Width="80" Margin="5"/>
            </StackPanel>
            <DataGrid ItemsSource="{Binding Vaccines}" SelectedItem="{Binding SelectedVaccine}" AutoGenerateColumns="False" Height="300">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding VaccineHistoryID}" Width="50"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="450"/>
                    <DataGridTextColumn Header="Quantity" Binding="{Binding Qty}" Width="*"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
