<UserControl x:Class="BholaCattleApp.ViewModels.AnimalRecordsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BholaCattleApp.ViewModels"
             mc:Ignorable="d" Background="White"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Animal" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
                <Button Content="Add" Command="{Binding AddCommand}" Width="80" Margin="5"/>
                <Button Content="Edit" Command="{Binding EditCommand}" Width="80" Margin="5"/>
                <Button Content="Delete" Command="{Binding DeleteCommand}" Width="80" Margin="5"/>
            </StackPanel>
            <DataGrid ItemsSource="{Binding Animals}" SelectedItem="{Binding SelectedAnimal}" AutoGenerateColumns="False" Height="300">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding AnimalID}" Width="50"/>
                    <DataGridTextColumn Header="Tag No" Binding="{Binding TagNumber}" Width="*"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Species" Binding="{Binding Species}" Width="*"/>
                    <DataGridTextColumn Header="Breed" Binding="{Binding Breed}" Width="*"/>
                    <DataGridTextColumn Header="Gender" Binding="{Binding Gender}" Width="*"/>
                    <DataGridTextColumn Header="DOB" Binding="{Binding DateOfBirth, StringFormat=dd/MM/yyyy}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
