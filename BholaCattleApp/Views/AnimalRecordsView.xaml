<UserControl x:Class="BholaCattleApp.ViewModels.AnimalRecordsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BholaCattleApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="60"/>
            <RowDefinition />
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Animal Records" FontSize="32" HorizontalAlignment="Center" FontWeight="Medium" FontFamily="Myriad Pro" Margin="15"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Add" Command="{Binding AddCommand}" Style="{StaticResource button}" Width="75" Margin="5"/>
            <Button Content="Edit" Command="{Binding EditCommand}" Style="{StaticResource button}" Width="75" Margin="5"/>
            <Button Content="Delete" Command="{Binding DeleteCommand}" Style="{StaticResource button}" Width="75" Margin="5"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Animals}" SelectedItem="{Binding SelectedAnimal}" Style="{DynamicResource DataGridStyle1}" CellStyle="{DynamicResource DataGridCellStyle1}" ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" RowStyle="{DynamicResource DataGridRowStyle1}"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" CanUserSortColumns="False" CanUserResizeColumns="False" CanUserReorderColumns="False" Margin="5" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Select" Width="80" CanUserResize="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      Margin="10 0 0 0" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Tag No" Binding="{Binding TagNumber}" Width="*"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Species" Binding="{Binding Species}" Width="*"/>
                <DataGridTextColumn Header="Breed" Binding="{Binding Breed}" Width="*"/>
                <DataGridTextColumn Header="Gender" Binding="{Binding Gender}" Width="*"/>
                <DataGridTextColumn Header="DOB" Binding="{Binding DateOfBirth, StringFormat=dd-MMM-yy}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Pagination bar -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="05">
            <Button Content="Previous" Command="{Binding PreviousPageCommand}" Style="{StaticResource button}" Width="90" Margin="5"/>
            <TextBlock Text="{Binding PageInfo}" VerticalAlignment="Center" FontSize="14" Margin="15,0"/>
            <Button Content="Next" Command="{Binding NextPageCommand}" Style="{StaticResource button}" Width="90" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>

