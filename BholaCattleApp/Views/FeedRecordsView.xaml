<UserControl x:Class="BholaCattleApp.Views.FeedRecordsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BholaCattleApp.Views"
             mc:Ignorable="d" Background="White"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Feeding" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
                <Button Content="Add" Command="{Binding AddFeedingCommand}" Width="80" Margin="5"/>
                <Button Content="Edit" Command="{Binding EditFeedingCommand}" Width="80" Margin="5"/>
                <Button Content="Delete" Command="{Binding DeleteFeedingCommand}" Width="80" Margin="5"/>
            </StackPanel>
            <DataGrid ItemsSource="{Binding Feedings}" SelectedItem="{Binding SelectedFeeding}" AutoGenerateColumns="False" Height="300">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding FeedingHistoryID}" Width="50"/>
                    <DataGridTextColumn Header="Animal ID" Binding="{Binding AnimalID}" Width="*"/>
                    <DataGridTextColumn Header="Status ID" Binding="{Binding StatusID}" Width="*"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*"/>
                    <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="*"/>
                    <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate, StringFormat=dd/MM/yyyy}" Width="*"/>
                    <DataGridTextColumn Header="End Date" Binding="{Binding EndDate, StringFormat=dd/MM/yyyy}" Width="*"/>
                    <DataGridTextColumn Header="Total Count" Binding="{Binding TotalCount}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
