<UserControl x:Class="BholaCattleApp.Views.AddEditAnimalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BholaCattleApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="{Binding Title}" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <!-- Binds to "Add Animal" or "Edit Animal" -->
            <TextBlock Text="Tag No:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding TagNumber}" Margin="0,0,0,10"/>
            <TextBlock Text="Name:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Name}" Margin="0,0,0,10"/>
            <TextBlock Text="Species:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Species}" Margin="0,0,0,10"/>
            <TextBlock Text="Breed:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Breed}" Margin="0,0,0,10"/>
            <TextBlock Text="Gender:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Gender}" Margin="0,0,0,10"/>
            <TextBlock Text="Date of Birth:" Margin="0,0,0,5"/>
            <DatePicker SelectedDate="{Binding DateOfBirth}" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Save" Command="{Binding SaveCommand}" Width="80" Margin="0,0,10,0"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
